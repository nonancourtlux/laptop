- name: First run 
  hosts: localhost
  tasks:
  - ansible.builtin.file:
      path: "{{item}}"
      state: touch 
    loop:
      - "/root/first_was_here_2"
    ignore_errors: true
  - git:
     repo: "{{ cc.git }}"
     dest: "/root/laptop"
