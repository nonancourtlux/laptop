- name: First run 
  hosts: localhost
  tasks:
  - ansible.builtin.file:
      path: "{{item}}" 
    loop:
      - "/root/first_was_here"
      - "/target/root/first_was_here_target"
    ignore_errors: true

